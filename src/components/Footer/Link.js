import styled from "styled-components";

const Link = styled.a`
  color: inherit;
  text-decoration: none;

  :hover {
    color: #de0000;
    text-decoration: underline;
  }
`;

export default Link;
